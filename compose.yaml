services:
  mysql:
    image: mysql:8.3
    environment:
      MYSQL_DATABASE: sareehub
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
